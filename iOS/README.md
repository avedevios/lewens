# iOS

This folder is reserved for iOS-specific documentation or assets.

Note: The active iOS project currently lives at the repository root in:
- lewens.xcodeproj
- lewens (SwiftUI app sources)

You can open lewens.xcodeproj in Xcode to build and run the app.
